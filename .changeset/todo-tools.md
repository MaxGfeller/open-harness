---
"@openharness/core": minor
"@openharness/react": minor
"@openharness/vue": minor
---

Add composable todo tools and UI state helpers. Core now ships session-scoped `todowrite` and `todoread` tools with pluggable todo storage and emits `oh:todo.updated` stream data; React and Vue expose `useTodos` helpers for rendering current todo state.
